folder = 'C:\Users\furka\Documents\GitHub\people-detection-hog-svm-matlab\positive';  
directory = dir(fullfile(folder,'*.jpg'));
trainingSe = imageDatastore(directory,   'IncludeSubfolders', true, 'LabelSource', 'foldernames');

for k = 1:numel(directory)
  filename = fullfile(folder,directory(k).name);
  img{k} = imread(filename);
end
length(img)
size(img)

